/**
 * ExpenseTracker App
 * A beautiful offline-first expense tracker with local storage and charts
 */

import React from 'react';
import AppNavigator from './src/navigation/AppNavigator';

export default function App() {
  return <AppNavigator />;
}
